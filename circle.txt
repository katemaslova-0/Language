знай-же r = 0;
знай-же x = 0;
знай-же y = 0;
знай-же c = 99;

отвечай-же-скорей(r);

покуда-льется-свет (c > 0)
{
    x = c - 10 * (c / 10);
    y = c / 10;

    сделаешь-шаг ( (x - 4) * (x - 4) + (y - 4) * (y - 4) <= r)
    {
        setpixel(x, y);
    };

    c = c - 1;
};
нарисуй;
